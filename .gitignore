# The manuscript is kept out of the repository while it is unsubmitted.
# src/10_figures.py and src/11_tables.py regenerate every figure and table
# into paper/ from the files in results/, so nothing here is lost.
paper/

# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# OS
Thumbs.db
.DS_Store

# Intermediate data regenerated by the pipeline (see README to rebuild).
# hourly_clean.csv is 8.7 MB and is fully reproducible from the raw input by
# src/01_clean.py; the smaller processed files are kept so the modelling stages
# can be run without re-downloading weather data.
data/hourly_clean.csv
